//variable in interface
interface A
{
int i = 10;
void test()
{
}
}
//inside interface we can only devlope constants and abstract method
//insede interface every method is abstract method
//all the member of interface are public by default